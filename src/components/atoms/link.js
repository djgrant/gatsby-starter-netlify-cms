import styled from '@emotion/styled';
import { typography, color, layout } from 'styled-system';
import GatsbyLink from 'gatsby-link';

const Link = styled(GatsbyLink)(typography, color, layout);

export default Link;
